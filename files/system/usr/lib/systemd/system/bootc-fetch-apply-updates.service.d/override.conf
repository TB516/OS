[Unit]
# This OS uses bootc's composefs backend, without /run/ostree-booted.
ConditionPathExists=
ConditionKernelCommandLine=composefs
Description=Stage bootc updates for the next reboot

[Service]
ExecStart=
ExecStart=/usr/bin/bootc upgrade --quiet
