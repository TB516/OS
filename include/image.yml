# Installers must also use this reference as the installed tracking origin.
variables:
  image-ref: ghcr.io/tb516/os:latest
  image-name: Personal OS
  image-id: personal-os
  image-version: '51.0'
  go-arch: amd64
